import SwiftUI

struct UnirseAGrupoPrivadoView: View {
    var body: some View {
        VStack {
            Text("Unirse a Grupo Privado")
                .font(.title)
                .padding()
            
            Spacer()
        }
        .navigationTitle("Unirse a Grupo Privado")
        .background(Color.white)
    }
}
